# Server-only. Replace this with the HTTPS URL supplied by the client.
# The endpoint receives JSON: { "email": "...", "name": "..." }.
# Blank optional names are omitted. No credentials belong in public variables.
WAITLIST_ENDPOINT={{WAITLIST_ENDPOINT}}
